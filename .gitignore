# Real site configs name your hosts / pin your models — only the .example files ship.
scripts/sync-commands.conf
scripts/review-backend.conf

# Runtime state the hooks/commands produce
handoffs/
.claude/plans/
tests/.tmp/

.DS_Store
